pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()  // Dodajte Google repozitorij
        mavenCentral()  // Dodajte Maven Central
        jcenter()  // Ako koristite JCenter, iako se polako povlači
        maven { url 'https://jitpack.io' }
    }
}
rootProject.name = "BuzzKeeper"
include ':app'
